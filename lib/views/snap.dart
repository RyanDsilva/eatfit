import 'package:flutter/material.dart';

class Snap extends StatelessWidget {
  const Snap({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Snap"),
    );
  }
}
