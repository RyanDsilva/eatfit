import 'package:flutter/material.dart';

class DoExercise extends StatelessWidget {
  const DoExercise({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Do Exercise"),
    );
  }
}
